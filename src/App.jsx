import ThemeProvider from "@/components/ThemeProvider";
import DynamicDataGrid from "./DynamicDataGrid";

const App = () => {
  return (
    <ThemeProvider>
      <DynamicDataGrid />
    </ThemeProvider>
  );
};

export default App;
